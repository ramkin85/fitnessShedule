'use strict';

describe('trainingShedule.shedule module', function() {

  beforeEach(module('trainingShedule.shedule'));

  describe('shedule controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});